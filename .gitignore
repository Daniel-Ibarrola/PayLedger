.idea/
prompts.md

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/

# Build output (lambda / layer zips)
build/

# Terraform
# The lock file is committed on purpose — it pins provider versions across
# machines and CI, which is the whole point of having one.
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
infra/build/

# HTTP client: holds the live API URL, generated by `make http-env`.
tests/http/http-client.private.env.json
